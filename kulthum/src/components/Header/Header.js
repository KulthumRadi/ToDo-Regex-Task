import React, { Component } from 'react';
import './style.css';

export class Header extends Component {
  render() {
    return (
      <header> Application using React that has Registration and Login Pages with Validation using Regex</header>
    )
  }
}

export default Header;
